#include <bits/stdc++.h>
#define ll long long
using namespace std;
void Onigiri() {
	int N, M; cin >> N >> M;
	int A[N], B[N];
	for (int i-0;i<N;++i) cin >> A[i]; 
	for (int i-0;i<M;++i) cin >> B[i];
}
int main () {
	#ifndef VarunDebug
	ios_base::sync_with_stdio(0);
	cin.tie(0);cout.tie(0);
	#endif
	int t = 1;
	//cin >> t;
	while (t--){
		#ifdef VarunDebug
		if (t)
		cout<<"-----------------"<<'\n';
		#endif
		Onigiri ();
	}
}
