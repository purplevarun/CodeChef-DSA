#include <iostream>
#include <cstring>
#include <unordered_map>
#define int long long
using namespace std;
int32_t main () {
    int t; cin >> t;
    while (t--){
        int N, K;
        cin >> N >> K;
        int A[N], dp[N]; // linear dp array to store length of subarrays at each index i
        for (int i=0;i<N;++i)
            cin >> A[i];
        
    }
}