#include <iostream>
#include <cstring>
#define ll long long
using namespace std;
int main () {
    int t; cin >> t;
    int N, K;
    int A[100007];
    int Flavours[100007];
    while (t--){
        cin >> N >> K;
        memset (Flavours, 0, sizeof(Flavours));
        for (int i=0;i<N;++i)
            cin >> A[i];
        
    }
}